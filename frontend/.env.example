# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Backend API URL
# This is the URL where the frontend will connect to the backend
BACKEND_URL=http://localhost:8000

# Cookie Encryption Key (REQUIRED for persistent login)
# Generate a secure key: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# DO NOT SHARE this key
COOKIE_ENCRYPTION_KEY=your-secure-cookie-encryption-key-here
